pipeline {
  agent any 
    stages {
       stage("scm poll/build") {
          steps{
        # hello build 
               }
       }   
       stage("test") {
          steps{
            # im in testing
               }
       }
       stage("deploy") {
           steps{
             # im in deploy statge
                }
       } 
    }
}
